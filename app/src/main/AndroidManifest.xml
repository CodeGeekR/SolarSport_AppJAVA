<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolarSport"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de RegisterUserActivity -->
        <activity
            android:name=".RegisterUserActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de ProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de CategoriesActivity -->
        <activity
            android:name=".CategoriesActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de BenefitsActivity -->
        <activity
            android:name=".BenefitsActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de StatisticsActivity -->
        <activity
            android:name=".StatisticsActivity"
            android:exported="true">
        </activity>

        <!-- Declaración de RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>